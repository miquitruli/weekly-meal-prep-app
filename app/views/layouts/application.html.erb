<!DOCTYPE html>
<html>
  <head>
    <title>WeeklyMealPrepApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= yield %>
    
        <% if logged_in? %>
                <nav>
                  You are logged in as: <%= link_to current_user.name, user_path(current_user) %> 
                  <p><%= link_to "Log Out", signout_path %> | 
                  <%= link_to "Dashboard" , dashboards_path%></p>
                </nav>    
        <% else %>
                <p><%= link_to "Register Now", register_path %> | 
                <%= link_to "Log in", login_path %></p>
        <% end %> 
  </body>
</html>
