<!DOCTYPE html>
<html>
  <head>
    <title>WeeklyMealPrepApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= yield %>
    
        <% if current_user %>
                <ul><%= link_to "Hello, #{current_user.username}!", user_path(current_user) %> | 
                <%= link_to "Log Out", signout_path %></ul>
        <% else %>
                <ul><%= link_to "Sign Up", new_user_path %> | 
                <%= link_to "Sign In", signin_path %></ul>
        <% end %>
   
  </body>
</html>
